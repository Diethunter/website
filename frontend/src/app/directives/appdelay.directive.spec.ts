import { AppdelayDirective } from './appdelay.directive';

describe('AppdelayDirective', () => {
  it('should create an instance', () => {
    const directive = new AppdelayDirective();
    expect(directive).toBeTruthy();
  });
});
